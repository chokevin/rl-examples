[project]
name = "rl-examples"
version = "0.1.0"
description = "Reinforcement learning examples powered by PufferLib"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "gymnasium==0.29.1",
    "pufferlib==3.0.0",
]

[tool.uv]
package = false
