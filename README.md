# rl-examples
Reinforcement learning examples powered by PufferLib

## PufferLib scaffold

This repo is currently scaffolded for examples, but does not implement any local
examples yet.

```bash
uv sync
uv run python examples/smoke_pufferlib.py
```

See `examples/README.md` for the current PufferLib pin and upstream example
workflow.
